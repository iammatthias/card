pub struct Card<'a> {
  pub name: &'a str,
  pub title: &'a str,
  pub handle: &'a str,
  pub company: &'a str,
  pub portfolio: &'a str,
  pub resume: &'a str,
  pub twitter: &'a str,
  pub github: &'a str
}
