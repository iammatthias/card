{
  "name": "iammatthias",
  "version": "0.6.58",
  "description": "npx business card",
  "main": "index.js",
  "bin": {
    "iammatthias": "./bin.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cargo:build": "cargo build --target=wasm32-unknown-emscripten --release",
    "wasm_pack:build": "cargo install wasm-pack && wasm-pack build --target nodejs --release",
    "mv": "mv ./target/wasm32-unknown-emscripten/release/card.* .",
    "mv_wasm": "mv ./pkg/card* .",
    "build": "yarn wasm_pack:build && yarn mv_wasm",
    "start": "node card.js"
  },
  "author": "iammatthias",
  "license": "MIT"
}
